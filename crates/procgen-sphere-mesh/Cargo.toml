[package]
name = "procgen-sphere-mesh"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
procgen-core = { path = "../procgen-core" }
rayon = "1"

[dev-dependencies]
procgen-sphere = { path = "../procgen-sphere" }
