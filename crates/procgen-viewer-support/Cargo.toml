[package]
name = "procgen-viewer-support"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = [
  "bevy_camera",
  "bevy_color",
  "std",
] }
bevy_egui = { version = "0.39.1", default-features = false, features = ["render"] }
